spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: testtesttest
    password: testtesttest
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 1000
          starttls:
            enable: true

  datasource:
    username: sa
    password:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=MySQL
  jpa:
    database: h2
    properties:
      hibernate:
        ddl-auto: create-drop
    show-sql: true
    generate-ddl: true
    database-platform: org.hibernate.dialect.H2Dialect
  data:
    redis:
      port: 6379
      host: localhost

  batch:
    job:
      enabled: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: something
            client-secret: something
            redirect-uri: something
            scope:
              - email
              - profile
          naver:
            client-id: something
            client-secret: something
            redirect-uri: something
            client-authentication-method: something
            authorizationGrantType: something
            clientName: something
            scope:
              - nickname
              - email
              - profile_image
          kakao:
            client-id: something
            client-authentication-method: something
            authorizationGrantType: something
            redirectUri: something
            scope:
              - profile_nickname
              - profile_image
            client-name: Kakao
        provider:
          naver:
            authorization-uri: something
            token-uri: something
            user-info-uri: something
            userNameAttribute: something
          kakao:
            authorization-uri: something
            token-uri: something
            user-info-uri: something
            userNameAttribute: something


# JWT 설정
secret:
  jwt-secret-key: testtesttesttesttesttesttesttesttest
  accessToken-valid-time: 1800000 # 1시간
  refreshToken-valid-time: 604800000 # 일주일

openai:
  model: gpt-3.5-turbo
  api:
    key: temp
    url: temp

cloud:
  aws:
    region: ap-northeast-2
    credentials:
      access-key: testtest
      secret-key: testtest
    s3:
      bucket: kuroom
    stack:
      auto: false
